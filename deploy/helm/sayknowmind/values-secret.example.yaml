# =====================================================================
# Copy this to values-secret.yaml and fill in real values.
# values-secret.yaml MUST be gitignored.
#
# Usage:
#   cp values-secret.example.yaml values-secret.yaml
#   # edit values-secret.yaml
#   helm install sayknowmind . -f values-production.yaml -f values-secret.yaml
#
# Generate strong random values with:
#   openssl rand -base64 32   # for *_SECRET / *_KEY
#   openssl rand -hex 32      # for ENCRYPTION_KEY / SEARXNG_SECRET_KEY / RELAY_SHARED_SECRET / TELEGRAM_WEBHOOK_SECRET
# =====================================================================

secrets:
  postgresPassword: ""
  betterAuthSecret: ""
  encryptionKey: ""
  relaySharedSecret: ""
  searxngSecretKey: ""
  aiApiKey: ""
  edgequakeApiKey: ""
  openaiApiKey: ""
  openrouterApiKey: ""
  telegramBotToken: ""
  telegramWebhookSecret: ""
