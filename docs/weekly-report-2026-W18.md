# SayKnowMind 周报

**周次**: 2026 年第 18 周
**周期**: 2026-04-27（周一）~ 2026-05-02（周六）
**项目**: SayKnowMind — Agentic Second Brain
**作者**: Kira ⚡

---

## 一、本周概览

本周共完成 **13 个提交** + **1 次完整回滚重做** + **1 个进行中的功能分支**，覆盖四大主题：

| 主题 | 提交数 | 状态 |
|------|--------|------|
| Telegram 集成稳定化 | 5 | 进行中（botId webhook 拆分） |
| 多用户隔离与安全 | 3 | 已完成 |
| 移动端/仪表盘 UX | 2 | 已完成 |
| 桌面端构建基础设施 | 1 | 已完成 |
| 维护性修复 | 2 | 已完成 |

---

## 二、按日工作日志

### 📌 4/27（周一）
- **`7a63297`** `fix(auth)`: 从 auth client 中移除孤立的 `jwtClient` 插件
  - 清理无用依赖，避免后续认证流程中的潜在冲突

---

### 📌 4/28（周二）— 高强度开发日

**当日先完成 7 个提交，随后整体 revert，再重新提交**（说明在合并/集成阶段发现问题，决策果断）。

| 提交 | 类型 | 说明 |
|------|------|------|
| `9943bcf` | feat(web) | 移动端 bottom-sheet 对话框 + "添加记忆" FAB 按钮 |
| `561b3bf` | feat(web) | 仪表盘统计卡片支持点击跳转 |
| `4001a66` | fix(web) | 强化 Telegram 数据接入 + 新增日语/中文保存触发器 |
| `1e70980` | fix(web) | 修复 document ingest 中 catch 块引用 try 作用域局部变量的 bug |
| `6bfb870` | fix(web) | Telegram typing keepalive + 视觉处理通过用户自有云提供商路由 |
| `dc4d8b8` | fix(web) | 在所有 AI processor 函数中贯穿 `userId` 参数 |
| `f02a359` | fix(web) | 限制并发 ingestion 任务数 + 强化重试路径 |
| `c83a699` | fix(web) | "Total Memories" 卡片改用 `totalCount` 字段 |

**🔍 重点说明**：
- **userId 贯穿改造**（`dc4d8b8`）：这是一次安全性重构 —— 确保 AI 处理流程全链路携带用户身份，为多租户隔离打下基础。
- **视觉处理路由**（`6bfb870`）：用户上传的图片不再走平台共享配额，而是走用户自己绑定的云提供商，体现"自带密钥"的产品哲学。

---

### 📌 4/29（周三）

| 提交 | 类型 | 说明 |
|------|------|------|
| `af76d33` | fix(web) | 防止 Telegram token 在用户之间发生交叉污染 |
| `ddcd473` | feat(desktop) | 桌面端添加 full/lite 双构建模式 + 支持 Windows 交叉编译 |

**🔍 重点说明**：
- **Token 隔离**（`af76d33`）：这是上一日 userId 改造的延续，关闭多用户场景下的安全漏洞。
- **桌面端双模式**（`ddcd473`）：
  - **Full 模式**：内嵌 Node.js + Next.js standalone 服务器，支持离线
  - **Lite 模式**：轻量 webview 壳，指向 mind.sayknow.ai
  - 同时引入 NSIS 安装器、平台感知辅助函数，并在受信任源中加入桌面端协议
  - **未验证**：从 macOS 主机交叉编译 Windows 包

---

### 📌 4/30（周四）

- **`318a13a`** `backup`: 合并上游前的本地工作树快照备份
- 配套 stash（`9e694a1`、`7a63297`）：合并期间自动更新 OMC 状态

合并风险管理意识良好，先做安全网再动手。

---

### 📌 5/1（周五）

休整日，无提交。

---

### 📌 5/2（周六，今日，进行中）

**正在拆分 Telegram webhook 路由架构**，工作树中存在以下变更（尚未提交）：

```
M apps/web/app/api/integrations/[channel]/route.ts
M apps/web/app/api/integrations/telegram/webhook/route.ts
M apps/web/components/settings/integrations-tab.tsx
?? apps/web/app/api/integrations/telegram/webhook/[botId]/route.ts  ← 新增
```

**推测意图**：从单一全局 webhook 切换到按 `botId` 分发的多 webhook 架构，进一步降低用户间的耦合。这是本周 Telegram 安全/隔离主题的延续。

---

## 三、本周关键洞察

### ✅ 做得好的地方

1. **安全意识贯穿全周**：从 userId 贯穿、token 隔离到 botId webhook 拆分，连续三天都在加固多用户隔离。
2. **决策果断**：4/28 一次性 revert 7 个提交后立即重做，没有拖泥带水。
3. **基础设施投入**：桌面端双模式构建为后续分发渠道铺平道路。

### ⚠️ 需要注意的地方

1. **Windows 交叉编译尚未验证**（`ddcd473` 的明确风险）—— 建议下周尽早在真实 Windows 环境跑一遍。
2. **未撰写单元测试覆盖**：本周大量 fix 是逻辑修正，但提交信息未提及测试，建议补充回归测试。
3. **5/2 的 webhook 拆分仍在进行**：注意收尾时验证旧 webhook 平滑迁移路径。

---

## 四、下周建议

| 优先级 | 任务 |
|--------|------|
| 🔴 高 | 完成 Telegram botId webhook 拆分并合并 |
| 🔴 高 | 在 Windows 真机验证桌面端 lite/full 构建 |
| 🟡 中 | 为本周 fix 类提交补充回归测试 |
| 🟡 中 | 整理移动端 bottom-sheet / FAB 的 UX 数据指标观察 |
| 🟢 低 | 文档化"用户自带密钥"的云提供商路由架构 |

---

## 五、本周主题总结

> 本周可以概括为 **"安全加固周"** —— 在产品功能向多用户场景演进的过程中，系统性地补齐了身份隔离链路。同时，移动端体验和桌面端发布渠道也获得了实质推进。

---

*报告生成时间：2026-05-02*
*Generated by Kira ⚡*
