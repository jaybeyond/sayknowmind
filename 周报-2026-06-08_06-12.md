# 周工作报告 · SayKnowMind

**周期：** 2026-06-08 ~ 2026-06-12
**项目：** SayKnowMind（智能第二大脑）
**分支：** `feat/collab-docs-mindmaps-rag`
**本周提交：** 35 个 commit

---

## 一、本周概述

本周聚焦三条主线：**协作文档编辑器的完善与上线**、**账户登录体系切换到 SayKnowWork SaaS**，以及**生产环境（EC2）的全量部署与一系列线上问题修复**。多标签文档/表格/思维导图编辑器、实时协作（Yjs/Hocuspocus）、应用内 AI 智能体均已部署上线并验证通过。

---

## 二、本周完成事项

### 1. 协作文档 / 多标签编辑器
- 多标签文档编辑器：支持**思维导图、HTML、Excel（Univer）**多种标签页
- 电子表格导出 **CSV / XLSX**，并实现**全保真 XLSX 导出**（样式、公式、合并单元格、几何尺寸）
- 共享内容只读渲染：笔记、电子表格、思维导图分享后以只读形式正确展示
- 共享电子表格以静态样式表格渲染（去除 Univer 依赖），自动裁剪到数据区并贴合表宽
- 表格 chrome 整合：标题移入应用头部、隐藏 Univer 工具栏（后按需恢复 ribbon）
- 为电子表格标签页增加标题字段；编辑器头部内置侧边栏开关
- 深色模式下 BlockNote 编辑器背景与页面统一；清理废弃的 doc-editor 组件

### 2. 实时协作（Yjs / Hocuspocus）
- relay-server 接入 **Hocuspocus 协作后端**，CRDT 状态持久化到 Postgres（`doc_collab`）
- 生产环境打通协作通道：nginx 新增 `/collab` WebSocket 路由、`NEXT_PUBLIC_COLLAB_WS_URL` 构建期注入
- 修复**协作标签页重复 Bug**：并发种子竞态导致重复"标签页 1"，改用确定性种子 id + 按 id 去重 + 自愈

### 3. 应用内 AI 智能体 / MCP
- 应用内智能体可**创建并分享**文档 / 表格 / 思维导图
- 智能体支持**中文与日文**指令（创建、分享意图识别）
- 分享意图按"消息中的标题"解析文档（与语言无关）；按内容推断文档类型并清洗标题
- MCP：支持通过 MCP **创建与编辑** Notion 文档 / 表格 / 思维导图并分享

### 4. 账户与登录（SaaS 集成）
- 登录改为**委托给 SayKnowWork SaaS**（后端代理校验，再签发本地会话），团队/权限/会话逻辑保持不变
- 屏蔽本地 HTTP 注册/登录端点，登录界面支持**工号或邮箱**

### 5. 数据可见性 / 团队
- 修复"加入团队功能后历史记忆全部不可见"的问题：**用户自己的记忆在任何激活组织下始终可见**（团队仅做加法，不隐藏个人数据）

### 6. UI / 交互改进
- 回收站新增**一键清空**（批量永久删除 + 二次确认）
- 头部"新建"与"添加记忆"合并为单一 **"+ 添加"下拉**
- 编辑器打开时隐藏仪表盘头部、修复面包屑泄漏、页面标题国际化、侧边栏开关

### 7. 知识图谱
- 修复图谱**自我反复重排（抖动）**：SSE 静默刷新数据未变化时不再触发模拟重启
- 修复**单节点缩放爆炸**（zoomToFit 在零面积包围盒下放大到铺满屏幕）
- 修复图谱**缓慢自动放大、漂移出屏**：消除 onEngineStop→状态→重算→重新加热的无限循环

### 8. 基础设施 / 运维
- 生产部署上线：EC2（经 JumpServer 堡垒机）本地构建 + docker compose，应用数据库迁移 **057~060**
- 修复 IPFS 服务状态误报（Kubo RPC 仅接受 POST，原 GET 返回 405 被判"不稳定"）
- 性能：移除仪表盘页面的 `force-dynamic`，恢复 Next `<Link>` 预取 → **菜单切换近乎瞬时**
- 修复 mcp-server 的 `package-lock.json` 与 `pg` 依赖不同步导致的 CI 构建失败

### 9. 国际化
- 新增/翻译 SaaS 登录、清空回收站、合并下拉等文案（**中 / 英 / 韩 / 日** 四语）

---

## 三、部署 / 上线状态

| 环境 | 状态 |
|---|---|
| 生产 EC2（`sayknowmind.ypai.click`） | ✅ 已全量上线并验证（登录、可见性、回收站、协作、图谱、IPFS 状态 4/4 在线） |
| GitLab `main` | ✅ 最新（`eefdaab`） |
| GitHub feature 分支 | ✅ 最新 |

---

## 四、遗留与下周计划

- **GitHub `main` 与 Railway**：GitHub main 因历史合并提交需要 force-push 或合并；Railway 是否继续作为部署目标待确认
- **密码找回流程**：SaaS 登录后本地改密/找回需打通（接入邮件服务）
- **Syncthing**：仅 Private 模式使用的设备间同步，云端模式无需；是否启用待定
- **历史重复标签清理**：线上仅 1 个文档存在重复标签，手动删除即可（修复后删除会生效）
- **多工作区切换**：accessToken 绑定单一 tenant，多工作区切换方案待与运维确认

---

*报告生成：2026-06-12 ⚡*
