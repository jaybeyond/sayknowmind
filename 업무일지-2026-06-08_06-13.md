# 주간 업무일지 · SayKnowMind

**기간:** 2026-06-08 ~ 2026-06-13
**프로젝트:** SayKnowMind (지능형 세컨드 브레인)
**브랜치:** `feat/collab-docs-mindmaps-rag`
**커밋:** 36 commit (+ 06-13 진행 중 미커밋 작업)

---

## 1. 이번 주 개요

세 갈래로 진행: **협업 문서 에디터 완성·배포**, **로그인 체계를 SayKnowWork SaaS로 전환**, **지식 그래프(Knowledge Graph) 사용성 대폭 개선 + 신규 기능(버전 기록) 구현**. 협업 문서/시트/마인드맵 에디터, 실시간 협업(Yjs/Hocuspocus), 앱 내 AI 에이전트는 프로덕션(EC2)에 배포·검증 완료. 후반부(06-13)는 로컬 환경에서 지식 그래프와 에디터 부가기능을 집중 보강.

---

## 2. 완료 사항

### 2-1. 협업 문서 / 멀티탭 에디터 (06-08~06-12)
- 멀티탭 에디터: **마인드맵 · HTML · Excel(Univer)** 탭 지원
- 시트 **CSV / XLSX 내보내기**, 스타일·수식·병합셀 포함 풀-피델리티 XLSX
- 공유 콘텐츠 읽기전용 렌더(노트/시트/마인드맵), 공유 시트는 정적 표로 렌더
- 시트 크롬 정리(제목을 앱 헤더로), 에디터 헤더에 사이드바 토글 내장
- 협업 탭 중복 버그 수정(seed 경쟁 → 결정적 id + 중복 제거 + 자가복구)

### 2-2. 실시간 협업 (Yjs / Hocuspocus)
- relay-server에 Hocuspocus 백엔드 연결, CRDT 상태를 Postgres(`doc_collab`)에 영속화
- 프로덕션 협업 채널 개통: nginx `/collab` WS 라우트, `NEXT_PUBLIC_COLLAB_WS_URL` 빌드 주입
- **협업 문서 헤더에 접속자(presence) 아바타** 추가 — collab-token이 user id+이미지 반환, awareness를 id로 dedupe해 헤더에 아바타 스택 (prod 배포 `e794c43`)

### 2-3. 앱 내 AI 에이전트 / MCP (06-10)
- 에이전트로 문서/시트/마인드맵 **생성·공유**, 한국어/중국어/일본어 지시 인식
- MCP를 통한 Notion 문서/시트/마인드맵 생성·편집·공유

### 2-4. 계정 / 로그인 (SaaS 통합) (06-10~06-12)
- 로그인을 **SayKnowWork SaaS에 위임**(백엔드 프록시 검증 후 로컬 세션 발급), 팀/권한/세션 로직 유지
- 단일 코드베이스 2개 에디션: `NEXT_PUBLIC_AUTH_MODE`로 Enterprise(saas) / Open(local) 분기

### 2-5. 지식 그래프 — 데이터 · 사용성 (06-13)
- 그래프 4종 노드(문서/엔티티/카테고리/태그) 노출 검증 + 로컬 데이터 시드(허브 포함)
- **노드 기본 크기 축소** + 자동맞춤(zoomToFit) 과확대 캡(`FIT_MAX_ZOOM`) — 노드 적을 때 거대해지던 문제 해결
- **수동 줌 무제한** (`maxZoom` 캡 제거, 자동맞춤에만 제한)
- **옵시디언식 노드 드래그**: 끌어서 위치 이동 + 시뮬레이션 reheat로 이웃 노드 추종 + 드롭 시 고정
- **드래그 위치 저장**(localStorage) — 새로고침해도 배치 유지
- 상세 패널 **미번역 라벨 전부 번역**: 노드 타입/엔티티 타입(`entityType` 값 포함)/속성 키(`propKey` 23종), 날짜 로케일 포맷, 내부 노이즈 키 숨김 — en/ko/zh/ja 4개 로케일

### 2-6. 버전 기록 (수정 시점 복원) — 신규 기능 (06-13)
- **노트 · 엑셀 · 마인드맵 전부** 자동 버전 캡처 (PATCH 한 곳에서, 30초 throttle + 내용 동일 시 스킵, 문서당 50개 retention, **작성자 기록**)
- 마이그레이션 `061_add_document_versions`, 캡처 헬퍼 `lib/versions/store.ts`
- API: 버전 목록(작성자 포함) + 복원(현재 상태 먼저 스냅샷 → 덮어쓰기 → collab CRDT 리셋 → 재시드)
- UI: 에디터 헤더 **🕘 버전 기록** 버튼 → 우측 패널(작성자 아바타·시각·**변경 diff: 추가/삭제 줄**·복원 버튼)

### 2-7. 버그 수정
- **insights 위젯이 항상 0** 으로 표시되던 근본 원인 수정 — pending-jobs 쿼리가 `$1`(userId)을 바인딩만 하고 안 써서 Postgres `42P18`(타입 미결정) → 전체가 silent catch로 0 반환. `$1::text IS NOT NULL` 앵커로 해결 + catch 에러 로깅 추가
- insights 갱신 트리거 추가(스토어 변화 + 탭 포커스 + 60초 폴링)
- 지식 그래프 자가-리세틀/단일노드 줌 폭주(06-10), 대시보드 즉시 내비(force-dynamic 제거), IPFS health POST

---

## 3. 배포

- **2026-06-13**: presence 아바타(`07bd1de → e794c43`) 프로덕션 배포 — gitlab main 푸시, EC2 호스트에서 빌드+재기동, https://sayknowmind.ypai.click 검증(HTTP 200, web healthy). 마이그레이션 없음, auth 동작 변화 없음.

---

## 4. 진행 중 / 후속 과제

- **로컬 presence 아바타 미표시 디버깅 중** — relay 측 WS 연결·인증은 정상 확인(`auth OK`), 클라이언트 awareness `user` 필드 표시 여부를 콘솔 로그로 진단 중
- 버전 기록: 동시 편집 중 복원 시 안전성(현재 단일 사용자 기준), 더 촘촘한 캡처/수동 저장 버튼 옵션 검토
- 06-13 로컬 작업분(지식 그래프 UX, i18n, insights 수정, 버전 기록) 커밋 및 배포 예정
